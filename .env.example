# Backend Port and Database URL
PORT=5080
DATABASE_URL="postgresql://root:root@localhost:15432/toktickit?schema=public"

# Host port for the PostgreSQL container (used by docker-compose.yml).
# Override if 15432 is already in use on your machine, and update
# DATABASE_URL above (and server/.env) to match.
DB_PORT=15432
